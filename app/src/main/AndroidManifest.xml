<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartrecipes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartRecipes">
        <activity android:name=".loginActivity"></activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".IngredientesAct" />
        <activity android:name=".RegistroIngredientes"></activity>
        <activity android:name=".EliminarIngrediente"></activity>
        <activity android:name=".RecetasDisponibles"></activity>
        <activity android:name=".Camarones"></activity>
        <activity android:name=".Spaguetti"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>